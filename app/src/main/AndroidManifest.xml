<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.digicus">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_icon"
        android:supportsRtl="true"
        android:screenOrientation="portrait"
        android:theme="@style/Theme.Digicus.FullScreen">
        <activity android:name=".UpdateDetails"></activity>
        <activity android:name=".UpdateHome" />
        <activity android:name=".SingleDetailsActivity" />
        <activity
            android:name=".DetailsActivity"
            android:theme="@style/Theme.Digicus.FullScreen" />
        <activity
            android:name=".SpashScreen"
            android:theme="@style/Theme.Digicus.FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.Digicus.FullScreen" />
    </application>

</manifest>